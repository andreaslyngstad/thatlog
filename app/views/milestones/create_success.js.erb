$(".flash_notice").empty()
$(".flash_notice").append("<%= escape_javascript(flash.discard(:notice))%>")
$("#new_milestone")[0].reset();
$("#listing_milestones").append("<%= escape_javascript(render(:partial => @milestone)) %>");
$(".open_milestone_update").UIdialogs_edit_links();
$(".spinning").hide();

