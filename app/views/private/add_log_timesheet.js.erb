$(".flash_notice").empty()
$(".flash_notice").append("<%= escape_javascript(flash.discard(:notice))%>");
$("#new_log")[0].reset();
$(".timesheet_table").replaceWith("<%= escape_javascript(render "private/timesheet_table") %>")
$(".spinning").hide();